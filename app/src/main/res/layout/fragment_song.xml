<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1D0010">

    <!-- Scrollable content -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="70dp"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Top Image Background -->
        <ImageView
            android:id="@+id/bgImage"
            android:layout_width="0dp"
            android:layout_height="320dp"
            android:scaleType="centerCrop"
            android:src="@drawable/happy"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Semi-transparent Overlay -->
        <View
            android:id="@+id/dimOverlay"
            android:layout_width="0dp"
            android:layout_height="320dp"
            android:background="#66000000"
            app:layout_constraintTop_toTopOf="@id/bgImage"
            app:layout_constraintBottom_toBottomOf="@id/bgImage"
            app:layout_constraintStart_toStartOf="@id/bgImage"
            app:layout_constraintEnd_toEndOf="@id/bgImage" />

        <View
            android:id="@+id/dimOverlay2"
            android:layout_width="0dp"
            android:layout_height="110dp"
            android:background="#55000000"
            app:layout_constraintTop_toTopOf="@id/emotionText"
            app:layout_constraintBottom_toBottomOf="@id/bgImage"
            app:layout_constraintStart_toStartOf="@id/bgImage"
            app:layout_constraintEnd_toEndOf="@id/bgImage" />

        <!-- Mood Text -->
        <TextView
            android:id="@+id/emotionText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginStart="25dp"
            android:fontFamily="@font/bebasneue_regular"
            android:text="Detected Mood : Happy"
            android:textColor="#E0E0E0"
            android:letterSpacing="0.01"
            android:textSize="32sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/playlistHeading"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Playlist Heading -->
        <TextView
            android:id="@+id/playlistHeading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginStart="25dp"
            android:paddingBottom="15dp"
            android:text="Recommended Songs"
            android:textColor="#E0E0E0"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/bgImage"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/songView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:padding="8dp"
            android:layout_marginTop="12dp"
            android:backgroundTint="#31011D"
            app:layout_constraintTop_toBottomOf="@id/playlistHeading"
            app:layout_constraintStart_toStartOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.core.widget.NestedScrollView>

    <!-- Fixed Header -->
<LinearLayout
android:id="@+id/header"
android:layout_width="match_parent"
android:layout_height="90dp"
android:orientation="horizontal"
android:background="#1D0010"
android:elevation="8dp"
    android:paddingTop="35dp">

<androidx.appcompat.widget.AppCompatImageButton
    android:id="@+id/btnback"
    android:layout_width="48dp"
    android:layout_height="48dp"
    android:src="@drawable/arrow"
    android:scaleType="fitCenter"
    android:layout_marginLeft="20dp"

    android:padding="12dp"
    android:background="@android:color/transparent"
    android:contentDescription="Back"
    app:tint="@android:color/white" />

<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="VibeZone"
    android:textColor="@color/white"
    android:fontFamily="@font/billabong"
    android:layout_marginStart="25dp"
    android:paddingTop="7dp"
    android:textSize="35sp"/>
</LinearLayout>
    </FrameLayout>
